<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
            android:id="@+id/bottom_navigation_video"
            android:enabled="true"
            android:icon="@drawable/ic_menu_video"
            android:title="@string/video"
            app:showAsAction="always|withText"/>

    <item
            android:id="@+id/bottom_navigation_gif"
            android:enabled="true"
            android:icon="@drawable/ic_menu_gif"
            android:title="@string/gif"
            app:showAsAction="always|withText" />

    <item
            android:id="@+id/bottom_navigation_me"
            android:enabled="true"
            android:icon="@drawable/ic_menu_me"
            android:title="@string/me"
            app:showAsAction="always|withText"/>
</menu>